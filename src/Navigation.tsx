export function Navigation({titles,index,go,fullscreen}:{titles:string[];index:number;go:(n:number)=>void;fullscreen:()=>void}) {
 return <><nav className="dots" aria-label="Navigace mezi snímky">{titles.map((title,i)=><button key={i} className={i===index?'active':''} aria-current={i===index?'step':undefined} aria-label={`${i+1}. ${title}`} onClick={()=>go(i)}><span className="dot"/><span className="dot-title">{title}</span></button>)}</nav><div className="controls"><button aria-label="Předchozí snímek" title="Předchozí snímek (←)" disabled={index===0} onClick={()=>go(index-1)}>←</button><button aria-label="Další snímek" title="Další snímek (→)" disabled={index===titles.length-1} onClick={()=>go(index+1)}>→</button><span/><button aria-label="Celá obrazovka" title="Celá obrazovka (F)" onClick={fullscreen}>⛶</button></div><div className="progress" aria-hidden="true"><div style={{width:`${(index+1)/titles.length*100}%`}}/></div></>
}
